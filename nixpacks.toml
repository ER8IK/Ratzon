[phases.setup]
nixPkgs = ["...", "nodejs_22"]

[phases.install]
cmds = [
  "python -m pip install -r requirements.txt",
  "cd frontend && corepack enable && corepack pnpm install --frozen-lockfile"
]

[phases.build]
cmds = ["cd frontend && corepack pnpm build"]

[start]
cmd = "python -m bot.main"
